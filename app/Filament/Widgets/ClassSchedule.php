<?php

namespace App\Filament\Widgets;

use App\Models\Course;
use Filament\Widgets\Widget;
use Carbon\Carbon;

//class ClassSchedule extends Widget
{
  //  protected static string $view = 'widgets.class-schedule';

//   public function getUpcomingClasses()
    {
      //  return Kelas::where('tanggal_mulai', '>=', Carbon::now())
      //      ->orderBy('tanggal_mulai', 'asc')
      //     ->limit(5)
      //      ->get();
    }
}
